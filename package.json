{
  "name": "zenith-task-monorepo",
  "version": "1.0.0",
  "description": "Decoupled MERN stack task tracker deployed as a single web service on Render",
  "main": "backend/server.js",
  "type": "module",
  "scripts": {
    "install-backend": "cd backend && npm install --include=dev",
    "install-frontend": "cd frontend && npm install --include=dev",
    "build-frontend": "cd frontend && npm run build",
    "build": "npm run install-backend && npm run install-frontend && npm run build-frontend",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=16"
  }
}
